buildscript {
    repositories {
        jcenter()
        maven { url "https://maven.google.com" } // добавьте это
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.3'
    }
}

allprojects {
    repositories {
        jcenter()
        maven { url "https://maven.google.com" } // добавьте это
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
